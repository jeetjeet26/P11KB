# Template for setting Supabase secrets
# Copy this file to set-secret.ps1 and replace YOUR_* placeholders with actual values
# NEVER commit the actual set-secret.ps1 file to the repository

$env:SUPABASE_ACCESS_TOKEN="YOUR_SUPABASE_ACCESS_TOKEN"
npx supabase secrets set OPENAI_API_KEY="YOUR_OPENAI_API_KEY" 